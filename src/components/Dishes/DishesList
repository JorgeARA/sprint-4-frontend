import React, { useState } from 'react';
import { Container, Stack, Row, Col } from 'react-bootstrap';
import './disheslist.css';
import { FcRating } from "react-icons/fc";
import { useNavigate } from 'react-router-dom';


export const DishesList = ({ arrayDishes: dishesArray }) => {

    const navigate = useNavigate();

    console.log('dishesArray');
    console.log(dishesArray);

    const handleClickRow = (e) => {
       console.log('click');
    };

    return (
        <>
            <Container fluid className='login_container'>
                <Stack>
                    {dishesArray.map((dishesObject, index) => {
                        return (
                            <>
                                <Row key={index} onClick={handleClickRow} >
                                    <Col>
                                        <img src={dishesObject.imagen} alt="restaurant" />
                                        <p>{dishesObject.name}</p>
                                        <p>${dishesObject.price}</p>
                                    </Col>
                                </Row>
                                <hr />
                            </>
                        )
                    })}
                </Stack>
            </Container>
            </>
    )
};
export default DishesList;